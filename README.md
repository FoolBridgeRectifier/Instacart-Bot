# Instacart-Bot

Right now works only for Windows but easily changed for other OS

Create app password on google and allow lesser security apps to allow emailing

Change credentials to check for required cart

Requires selenium module - python
